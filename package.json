{
  "name": "ui",
  "license": "MIT",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "lerna run --stream build",
    "build-local": "lerna run --stream build-local",
    "build-storybook": "lerna run --stream build-storybook",
    "dev": "lerna run --stream dev",
    "install-all": "lerna run --stream install",
    "start": "lerna run --parallel --stream start",
    "storybook": "lerna run --parallel --stream storybook",
    "stryker": "lerna run --parallel --stream stryker",
    "test": "lerna run --stream --no-bail test"
  },
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "lerna": "^9.0.7"
  },
  "resolutions": {
    "axios": "^1.16.0",
    "brace-expansion": "^2.0.2",
    "follow-redirects": "^1.16.0",
    "serialize-javascript": "^6.0.3",
    "yaml": "^2.8.4"
  },
  "overrides": {
    "axios": "^1.16.0",
    "brace-expansion": "^2.0.2",
    "follow-redirects": "^1.16.0",
    "serialize-javascript": "^6.0.3",
    "yaml": "^2.8.4"
  },
  "packageManager": "yarn@4.2.2"
}
